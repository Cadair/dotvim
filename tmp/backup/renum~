#!/bin/bash
#bash script to renumber files in a directory to start at 1

i=$1
for f in *jpg
do
    i=$(printf %04d $i)
    mv -v "$f" "tmp_img_$i"
    let i=10#$i+1
done


for f in tmp*
do
    fname=${f#*tmp_}.jpg    
    mv -v $f $fname
done
